# rabby (v2 acceptance fixture)

Rabby is a **v2 acceptance target** for `tauri-plugin-extensions`
(see `../../../docs/DECISIONS.md` D-005). The unpacked extension
contents are distributed under Rabby's license and are **not
committed** here; only this README is tracked.

## Populate

From the repo root, on Windows PowerShell:

```powershell
pwsh -File scripts/fetch-rabby.ps1
```

CWS id: `acmacodkjbdgmoleebolmdjonilkdbch`.
