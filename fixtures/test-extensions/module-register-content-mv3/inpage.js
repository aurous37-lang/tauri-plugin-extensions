// MAIN-world inpage script registered at runtime by the module SW via
// chrome.scripting.registerContentScripts. The dapp canary checks for this
// global to confirm the registration reached the page.
window.__fixtureMainWorldInjected = true;
